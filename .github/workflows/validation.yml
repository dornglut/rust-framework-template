name: Rust Framework Template Validation

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: rust-framework-template-validation-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate framework template
    uses: dornglut/github-workflows/.github/workflows/reusable-rust-cargo-validate.yml@688f274ec1fdd19acba9bf3577b26b4b7b7f4037
